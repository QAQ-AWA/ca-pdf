# Frontend variables consumed during the container build
# Copy to frontend.env and adjust per environment before deploying.

FRONTEND_VITE_APP_NAME=Monorepo UI
FRONTEND_VITE_API_BASE_URL=https://api.example.com
